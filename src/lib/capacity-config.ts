/** Default weekly availability per team member (hours). */
export const DEFAULT_WEEKLY_HOURS = 40;

/** Fallback tickets/week when no throughput data exists. */
export const DEFAULT_TICKETS_PER_WEEK = 5;
